CancelMarketOrder_Integration_Test
├── given the sender is not the owner of trading account
│   └── it should revert
└── given the sender is the owner of trading account
    ├── given there's not a market order
    │   └── it should revert
    └── given there's a market order
        ├── it should cancel the active market order
        └── it should emit {LogCancelMarketOrder} event
