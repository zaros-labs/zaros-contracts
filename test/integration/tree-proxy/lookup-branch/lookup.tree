Lookup_Integration_Test
├── given the sender is not the owner
│   └── it should revert
└── given the sender is the owner
    ├── given called branches function
    │   ├── it should return the branches
    │   └── it should return the selectors
    ├── given called branchFunctionSelectors function
    │   └── it should return the selectors of branch
    ├── given called branchAddresses
    │   └── it should return all addresses of branches
    └── given branchAddress function
        └── it should return the branch address
