UsdTokenSwapKeeper_UpdateConfig_Integration_Test
└── given initialize contract
    ├── when address of market making engine is zero
    │   └── it should revert
    └── when address of market making engine is valid
        ├── when address of asset is zero
        │   └── it should revert
        └── when address of asset is not zero
            ├── when stream id is zero
            │   └── it should revert
            └── when stream id is not zero
                └── it should update the config
