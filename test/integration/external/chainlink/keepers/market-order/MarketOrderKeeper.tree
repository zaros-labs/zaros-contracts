MarketOrderKeeper_Integration_Test
├── given initialize contract with some wrong information
│   ├── when address of perps engine is zero
│   │   └── it should revert
│   ├── when address of fee recipient is zero
│   │   └── it should revert
│   ├── when market id is zero
│   │   └── it should revert
│   └── when stream id is zero
│       └── it should revert
└── given initialize contract
    ├── given call getConfig function
    │   ├── it should return keeper owner
    │   ├── it should return address of perps engine
    │   ├── it should return address of fee recipient
    │   └── it should return market id
    ├── given call updateConfig function
    │   ├── when call updateConfig and address of perps engine is zero
    │   │   └── it should return
    │   ├── when call updateConfig and address of fee recipient is zero
    │   │   └── it should return
    │   ├── when call updateConfig and market id is zero
    │   │   └── it should return
    │   ├── when call updateConfig and stream id is zero
    │   │   └── it should return
    │   └── it should update the config
    ├── given call performUpkeep function
    │   └── it should emit {LogSettleOrder} event
    ├── given call checkLog function
    │   └── it should revert
    └── given call checkCallback function
        ├── it should return upkeepNeeded
        └── it should return performData
